[package]
name = "sidequest-cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[[bin]]
name = "sq"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sidequest-core = { path = "../core" }

[dev-dependencies]
tempfile = "3"

[lints]
workspace = true
